[
  {
    "type": "function",
    "name": "assess_batch_search",
    "description": "Find the target or select one visually grounded contextual clue from a camera sweep.",
    "parameters": {
      "type": "object",
      "properties": {
        "result": {
          "type": "string",
          "description": "found means the requested target is definitively visible; candidate means one specific but not yet definitive piece of evidence is visible; not_found means neither the target nor useful evidence is visible.",
          "enum": [
            "found",
            "candidate",
            "not_found"
          ]
        },
        "candidate_type": {
          "description": "For candidate only: visual means an object closely matches the target visually, or a target-specific part is visible, but the target cannot yet be confirmed. local means specific target-linked evidence is visible nearby, and a short movement may reveal the target; generic containers or ordinary nearby objects are not local evidence. destination means a recognized or mapped place or route has a strong semantic relationship to the target, such as a recognized kitchen when searching for a fridge; an unidentified hallway alone is not a destination. speculative means only a generic possibility with no target-specific evidence. Speculative clues are stored while the robot explores unseen local space first. Use visual, local, or destination only when the visible evidence closely supports that definition; otherwise use speculative. Use null for found and not_found.",
          "type": ["string", "null"],
          "enum": ["visual", "local", "destination", "speculative", null]
        },
        "candidate_image": {
          "type": "string",
          "description": "For found, the image containing the target. For candidate, the image containing the contextual clue. Use none for not_found.",
          "enum": [
            "image_1",
            "image_2",
            "image_3",
            "none"
          ]
        },
        "target_position": {
          "description": "For found only, a normalized point touching the target in candidate_image, using exactly x and y. Use null for candidate and not_found.",
          "type": [
            "object",
            "null"
          ],
          "properties": {
            "x": {
              "type": "number",
              "minimum": 0,
              "maximum": 1
            },
            "y": {
              "type": "number",
              "minimum": 0,
              "maximum": 1
            }
          },
          "required": [
            "x",
            "y"
          ],
          "additionalProperties": false
        },
        "contextual_clue": {
          "type": ["string", "null"],
          "description": "For candidate only, a concise description of the visible clue, where it is, and why moving toward it may reveal the target. Use null for found and not_found."
        }
      },
      "required": [
        "result",
        "candidate_type",
        "candidate_image",
        "target_position",
        "contextual_clue"
      ],
      "additionalProperties": false
    }
  },
  {
    "type": "function",
    "name": "assess_frame_search",
    "description": "Find the target or identify one useful contextual clue in a single camera frame.",
    "parameters": {
      "type": "object",
      "properties": {
        "result": {
          "type": "string",
          "description": "found means the requested target is definitively visible; candidate means one specific but not yet definitive piece of evidence is visible; not_found means neither exists.",
          "enum": [
            "found",
            "candidate",
            "not_found"
          ]
        },
        "candidate_type": {
          "description": "For candidate only: visual means an object closely matches the target visually, or a target-specific part is visible, but the target cannot yet be confirmed. local means specific target-linked evidence is visible nearby, and a short movement may reveal the target; generic containers or ordinary nearby objects are not local evidence. destination means a recognized or mapped place or route has a strong semantic relationship to the target, such as a recognized kitchen when searching for a fridge; an unidentified hallway alone is not a destination. speculative means only a generic possibility with no target-specific evidence. Speculative clues are stored while the robot explores unseen local space first. Use visual, local, or destination only when the visible evidence closely supports that definition; otherwise use speculative. Use null for found and not_found.",
          "type": ["string", "null"],
          "enum": ["visual", "local", "destination", "speculative", null]
        },
        "target_position": {
          "description": "For found only, a normalized point touching the target, using exactly x and y. Use null otherwise.",
          "type": ["object", "null"],
          "properties": {
            "x": {"type": "number", "minimum": 0, "maximum": 1},
            "y": {"type": "number", "minimum": 0, "maximum": 1}
          },
          "required": ["x", "y"],
          "additionalProperties": false
        },
        "contextual_clue": {
          "type": ["string", "null"],
          "description": "For candidate only, concisely describe the visible evidence, its direction, and the target-specific reason for its category. Use null otherwise."
        }
      },
      "required": [
        "result",
        "candidate_type",
        "target_position",
        "contextual_clue"
      ],
      "additionalProperties": false
    }
  },
  {
    "type": "function",
    "name": "select_navigation_pose",
    "description": "Assess overall navigation progress from fresh map and camera evidence, then either choose one safe local pose or end with a reason.",
    "parameters": {
      "type": "object",
      "properties": {
        "decision": {
          "type": "string",
          "enum": [
            "move",
            "goal_reached",
            "blocked"
          ],
          "description": "move selects another local waypoint; goal_reached confirms the complete query is satisfied; blocked means no listed pose can be chosen, including when evidence is insufficient."
        },
        "pose_id": {
          "type": [
            "string",
            "null"
          ],
          "description": "Exact candidate ID for decision=move. Numeric IDs are local translation poses, F-prefixed IDs are frontier goals, and R-prefixed IDs rotate in place. Use only an ID listed for the current snapshot. Use null for every terminal decision."
        },
        "reason": {
          "type": "string",
          "description": "Brief evidence for the choice, or why no pose is suitable."
        }
      },
      "required": [
        "decision",
        "pose_id",
        "reason"
      ],
      "additionalProperties": false
    }
  }
]
